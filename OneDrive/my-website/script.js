// Contoh interaktivitas jika diperlukan
document.querySelectorAll('.module a').forEach(link => {
    link.addEventListener('click', () => {
        alert("Anda akan diarahkan ke YouTube");
    });
});
